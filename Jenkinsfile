pipeline {
  agent any
  stages {
    stage('build') {
      steps {
        echo 'build has to be complete'
      }
    }
    stage('test') {
      steps {
        echo 'testing has to be performed'
      }
    }
    stage('deploy') {
      steps {
        echo 'deploy to be done'
      }
    }
  }
}